package com.dife.community.community.repository;

import com.dife.community.model.ClippedHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClippedHistoryRepository extends JpaRepository<ClippedHistory, Long> {
}
