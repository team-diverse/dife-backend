package com.dife.community.community.repository;

import com.dife.community.model.ClipedHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClipedHistoryRepository extends JpaRepository<ClipedHistory, Long> {
}
