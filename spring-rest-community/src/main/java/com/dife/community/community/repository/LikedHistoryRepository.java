package com.dife.community.community.repository;

import com.dife.community.model.LikedHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LikedHistoryRepository extends JpaRepository<LikedHistory, Long> {
}
