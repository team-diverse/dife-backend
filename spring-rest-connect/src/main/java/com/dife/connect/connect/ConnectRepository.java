package com.dife.connect.connect;

import com.dife.connect.model.Connect;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ConnectRepository extends JpaRepository<Connect, Long> {
}
