# database
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 본인이 생성한 table 입력
    url: jdbc:mysql://localhost:3306/dife_test?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&serverTimezone=Asia/Seoul
    # MySQL username, password 입력
    username: root
    password: 'gusuyon04'

  # spring data jpa ??
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create

