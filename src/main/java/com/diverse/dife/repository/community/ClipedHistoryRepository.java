package com.diverse.dife.repository.community;

import com.diverse.dife.entity.community.ClipedHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClipedHistoryRepository extends JpaRepository<ClipedHistory, Long> {
}
